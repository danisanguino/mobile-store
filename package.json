{
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && pnpm run dev",
    "dev:both": "npm-run-all --parallel dev:backend dev:frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "npm-run-all": "^4.1.5"
  }
}
